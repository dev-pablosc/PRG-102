// Crear Array (2)

// usando Corchetes[]
let numeros = [2, 5, 1, 0];
console.log(numeros)
console.table(numeros)

// 2da () >> no tan comun
const dias = new Array('lunes', 'martes', 'jueves')
console.log(dias)

// Arrays almacena varios datos, y TIPOS de datos
let variado = [2, 'Hola', true, undefined, dias, {nombre: 'Pablo', lenguaje: 'JS'}]
console.log(variado)
